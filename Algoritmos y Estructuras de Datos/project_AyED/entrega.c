#include "entrega.h"
#include "pruebas.h"
#include <stdio.h>

int ejercicioMenu()
{
    /*int opcion;
    int subOpcion;
    int duplicado;
    do{
        imprimirMenu();
        scanf("%d",&opcion);
        switch(opcion)
        {
            case 1:
            case 2:
                do
                {
                    subMenu();
                    scanf("%d",&subOpcion);
                    switch(subOpcion)
                    {
                        case 1:
                            duplicado = CON_DUPLICADO;
                            break;
                        case 2:
                            duplicado = SIN_DUPLICADO;
                            break;
                        default:
                            Error();
                    }
                }
                while(subOpcion!=1 && subOpcion!=2);
                if(opcion==1)
                    ///realizar accion con datos simples
                if(opcion==2)
                    ///realizar accion con datos duplicados
            break;
            case 3:
                puts("Saliendo del programa...");
                break;
            default:
                Error();
            }
        }while(opcion!=3);
    printf("Fin del programa...");*/
    return 0;
}
